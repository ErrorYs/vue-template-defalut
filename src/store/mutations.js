// 直接更新state的多个方法的对象
import {
// RECEIVE_SWIPER,
} from './mutations-type'
// import Vue from 'vue'

export default {
  // [RECEIVE_SWIPER] (state, { swiper }) {
  //   state.swiper = swiper
  // },
}
